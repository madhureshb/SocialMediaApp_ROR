<nav class="navbar navbar-expand-lg navbar-light bg-light">
        <div class="container">
        <%= link_to 'Instagram', root_path, class: 'navbar-brand' %>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="top-top">
            <% if current_account %> 
            <div class="collapse navbar-collapse" id="navbarSupportedContent ">
              <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                  <%= link_to 'Home', root_path, class: 'nav-link' %> 
                </li>
                
                <li class="nav-item pad-surname <%= 'active' if request.path == my_friends_path %>">
                  <%= link_to 'My Friends', my_friends_path, class: 'nav-link'  %>
                </li>

                <li class="nav-item">
                  <%= link_to 'New Posts', new_post_path(account_id: current_account), class: 'nav-link'  %>
                </li>

                <li class="nav-item">
                  <%= link_to 'Dashboard', dashboard_path(account_id: current_account), class: 'nav-link'  %>
                </li>
                
                <li class="nav-item dropdown">
                          <a class="nav-link dropdown-toggle" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                          Profile
                          </a>
                          <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                          
                          <%= link_to edit_account_registration_path, class: 'dropdown-item' do %>
                              Edit profile
                          <% end %>
                          </div>
                </li>
              </ul>    
              <ul class="navbar-nav ml-auto"> 
                <li class="nav-item user-name pad-name">
                <%= fa_icon 'user' %> <%= current_account.full_name %>
                </li>
                <li class='nav-item'>
                  <%= link_to("Logout", destroy_account_session_path, 
                  class: "btn btn-danger logout-button", method: :delete)%>
                </li>
              </ul>    
              
          </div>
          <% else %>
          <div class="collapse navbar-collapse" >
            <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                  <%= link_to("Login", new_account_session_path,
                              class: "btn btn-success login-button", method: :get)%>
            </li>
          </ul>
          </div>
        <% end %>
      </div>
      </div>
    </nav>